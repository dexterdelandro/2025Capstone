using UnityEngine;

public class preserveAudio : MonoBehaviour
{
    public static preserveAudio instance { get; private set; }

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
